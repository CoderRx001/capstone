<!-- require 'colorize'
 -->
<div class="ingredients">
  

<h2><%= @recipe.title %></h2>

 <h5>Prep Time: <%= @recipe.prep_time %></h5>

<h3>Ingredients</h3>
  <ul>
    <% @ingredients.each do |ingredient| %>
      <% if @intersection.include? ingredient.name  %>
        <li><%= highlight(ingredient.name,ingredient.name) %></li>
      <% else %>
        <li><%= ingredient.name %></li>
       <% end %>
    <% end %>
  </ul>

  
<h3>Directions</h3>
<ol>
  <% @recipe.directions.split(", ").each do |direction| %>
    <li><%= direction %></li>
  <% end %>
</ol>

<h3>Your foods v Recipe</h3>
<p><%= "#{@intersection_count} / #{@ingredients.count}" %></p>


<%= link_to "Edit", edit_recipe_path(@recipe), class: "btn btn-primary" %> |

<%= link_to "Delete", "/recipes/#{ @recipe.id }", method: :delete, class: "btn btn-danger" %>

</div> 
