
<h2><%= @recipe.title %></h2>

 <h5>Prep Time: <%= @recipe.prep_time %></h5>

<h3>Ingredients</h3>
  <ul>
    <% @recipe.ingredients.split(", ").each do |ingredient| %>
      <li><%= ingredient %></li>
    <% end %>
  </ul>

<h3>Directions</h3>
<ol>
  <% @recipe.directions.split(", ").each do |direction| %>
    <li><%= direction %></li>
  <% end %>
</ol>

<h3>Recipe VS foods</h3>
<p><%= "#{@ingredients.count} / #{@intersection_count}" %></p>

<%= link_to "Edit", edit_recipe_path(@recipe), class: "btn btn-primary" %> |

<%= link_to "Delete", "/recipes/#{ @recipe.id }", method: :delete, class: "btn btn-danger" %>
